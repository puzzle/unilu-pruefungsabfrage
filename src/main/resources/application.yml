server:
  forward-headers-strategy: framework
  unauthorized.urls: "/home /error /return"
  error.path: redirect:/error
  servlet:
    session:
      cookie:
        same-site: strict

spring:
  application:
    name: exam-feedback-tool
    version: @project.version@
